<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".app.FontoApplication"
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fonto.Splash"
        android:usesCleartextTraffic="true">

        <profileable android:shell="true" />

        <receiver
            android:name=".common.app.broadcast.MarkAsReadBroadcastReceiver"
            android:exported="true">

            <intent-filter>
                <action android:name="dev.weazyexe.fonto.actions.ACTION_MARK_AS_READ" />
            </intent-filter>

        </receiver>

        <activity
            android:name=".ui.AppActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>